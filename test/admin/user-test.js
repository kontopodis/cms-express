const log = require("../../tools/log");
const buildMakeUser = require("../../routes/admin/user/user");
const Pass = require("../../routes/admin/checkers/password");
const Id = require("../../routes/admin/checkers/id");
var { expect, assert } = require("chai");

module.exports = ()=>{
    
}